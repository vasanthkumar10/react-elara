// import React from "react"; // Not required

// whenever you are using some state to store memory -> import react
// Else -> its not required
// Use JSX -> html + js are used

// stateless functional component
export function Welcome(props) {
  return <h2>Welcome to Newton School!!! {props.name}</h2>;
}
